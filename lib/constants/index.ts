const IFRAME_CONTAINER_ID = `solid-payment-form-container_#${
  Math.random() * 10000
}`
const GOOGLE_PAY_BUTTON_CONTAINER_ID = `google-pay-button-container_#${
  Math.random() * 10000
}`
const APPLE_PAY_BUTTON_CONTAINER_ID = `apple-pay-button-container_#${
  Math.random() * 10000
}`

const PAYPAL_BUTTON_CONTAINER_ID = `paypal-button-container_#${
  Math.random() * 10000
}`

const PIX_BUTTON_CONTAINER_ID = `pix-button-container_#${Math.random() * 10000}`

const BIZUM_BUTTON_CONTAINER_ID = `bizum-button-container_#${
  Math.random() * 10000
}`

const BLIK_BUTTON_CONTAINER_ID = `blik-button-container_#${
  Math.random() * 10000
}`

const MBWAY_BUTTON_CONTAINER_ID = `mbway-button-container_#${
  Math.random() * 10000
}`

const PIX_QR_BUTTON_CONTAINER_ID = `pix-qr-button-container_#${
  Math.random() * 10000
}`

const CASHAPP_BUTTON_CONTAINER_ID = `cashapp-button-container_#${
  Math.random() * 10000
}`

const PIX_AUTOMATICO_BUTTON_CONTAINER_ID = `pix-automatico-button-container_#${
  Math.random() * 10000
}`

export {
  IFRAME_CONTAINER_ID,
  GOOGLE_PAY_BUTTON_CONTAINER_ID,
  APPLE_PAY_BUTTON_CONTAINER_ID,
  PAYPAL_BUTTON_CONTAINER_ID,
  PIX_BUTTON_CONTAINER_ID,
  BIZUM_BUTTON_CONTAINER_ID,
  BLIK_BUTTON_CONTAINER_ID,
  MBWAY_BUTTON_CONTAINER_ID,
  PIX_QR_BUTTON_CONTAINER_ID,
  CASHAPP_BUTTON_CONTAINER_ID,
  PIX_AUTOMATICO_BUTTON_CONTAINER_ID
}
